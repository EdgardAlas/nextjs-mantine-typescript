import { Layout } from '@/components/layout';
import { Title } from '@/components/override';

const Home = () => {
  return (
    <Layout>
      <Title align='center' mt={100}>
        NextJS + Mantine + Typescript
      </Title>
    </Layout>
  );
};

export default Home;
